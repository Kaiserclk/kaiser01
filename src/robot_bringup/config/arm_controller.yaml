controller_manager:
  ros__parameters:
    arm_position_controller:
      type: forward_command_controller/ForwardCommandController

    arm_controller:
      type: joint_trajectory_controller/JointTrajectoryController

    hand_controller:
      type: position_controllers/GripperActionController

arm_position_controller: # 机械臂关节位置控制
  ros__parameters:
    joints:
      - arm_joint1
      - arm_joint2
      - arm_joint3
      - arm_joint4
      - arm_joint5
      - arm_joint6
    interface_name: position

arm_controller: # 机械臂关节轨迹控制器
  ros__parameters:
    joints:
      - arm_joint1
      - arm_joint2
      - arm_joint3
      - arm_joint4
      - arm_joint5
    command_interfaces:
      - position
    state_interfaces:
      - position
hand_controller: # Gripper controller for hand joint
  ros__parameters:
    joint: arm_joint6
