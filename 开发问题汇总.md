# 次文档记录开发过程中遇到的问题，方便后续复盘总结


## 开发问题

### 问题1: 机械臂拖动时关节状态不实时更新
**日期**: 2026-06-28
**模块**: `robot_board_hardware/src/robot_board_system_interface.cpp`
**问题描述**:
- 关闭舵机力矩后手动拖动机械臂,`/joint_states` 和 `/dynamic_joint_states` 话题的角度没有实时更新
- 机械臂状态始终显示最后一个命令值,不跟随实际位置变化

**根本原因**:
- `read()` 函数中机械臂关节位置只是**回写命令值**,没有从硬件读取实际位置:
  ```cpp
  // ❌ 错误实现
  hw_states_[pos_key] = hw_commands_[pos_key];
  ```
- 拖动时舵机实际位置改变了,但 `hw_commands_` 不变,导致 `hw_states_` 也不变

**解决方案**:
1. 修改 `read()` 函数,主动从舵机读取实际位置:
   ```cpp
   // ✅ 正确实现
   auto pulse_opt = serial_port_->read_bus_servo_position(sc.servo_id);
   if (pulse_opt.has_value()) {
     double actual_rad = sc.pulse_to_rad(pulse_opt.value());
     hw_states_[pos_key] = actual_rad;
     hw_commands_[pos_key] = actual_rad;  // 同步命令值
   }
   ```

2. 修复初始化姿态不生效问题:
   - 问题: `on_activate()` 发送初始化命令后,舵机需要1200ms移动,但第一个 `read()` 调用时舵机还没到位
   - 修复: 在 `on_activate()` 中等待舵机移动完成后再继续
   ```cpp
   // 等待舵机移动完成
   std::this_thread::sleep_for(std::chrono::milliseconds(duration + 200));
   ```

**经验教训**:
- ✅ 硬件接口的 `read()` 函数必须**从硬件读取真实状态**,不能只回写命令值
- ✅ `on_activate()` 中发送初始化命令后,需要等待硬件执行完成才能返回
- ✅ 力矩关闭时,舵机位置由外部拖动改变,必须实时读取硬件状态
- ✅ 轮子关节可以积分速度估算位置,但舵关节必须直接读取

**相关文件**:
- `src/robot_board_hardware/src/robot_board_system_interface.cpp` - 硬件接口实现
- `src/robot_board_hardware/include/robot_board_hardware/serial_port.hpp` - 串口读取接口
- `src/robot_bringup/config/robot_controllers.yaml` - joint_state_broadcaster配置

---

### 问题2: joint_state_broadcaster 配置警告
**日期**: 2026-06-28
**模块**: `robot_bringup/config/robot_controllers.yaml`
**问题描述**:
- 配置了 `joints` 参数但仍出现警告: `'joints' or 'interfaces' parameter is empty`

**根本原因**:
- `joint_state_broadcaster` 源码逻辑(第406-409行):
  ```cpp
  bool use_all_available_interfaces() const {
    return params_.joints.empty() || params_.interfaces.empty();  // OR逻辑
  }
  ```
- **必须同时配置** `joints` 和 `interfaces` 两个参数,任一为空就会使用全部接口并警告

**解决方案**:
```yaml
joint_state_broadcaster:
  ros__parameters: 
    joints:
      - wheel_left_front_joint
      - wheel_left_back_joint
      - wheel_right_front_joint
      - wheel_right_back_joint
      - arm_joint1
      - arm_joint2
      - arm_joint3
      - arm_joint4
      - arm_joint5
      - arm_joint6
    interfaces:
      - position
      - velocity
```

**经验教训**:
- ✅ 配置 `joint_state_broadcaster` 时,`joints` 和 `interfaces` 必须**同时存在**
- ✅ 接口映射警告(”Mapping from 'position' to interface 'position' will not be done")是正常的,因为直接使用了标准接口名

---

### 问题3: 没有初始化机械臂
